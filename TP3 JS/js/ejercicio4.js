'use strict'

const parImpar = (num) => {
    if (num % 2 === 0 ) {
        console.log(`El numero ${num} es par`);;
    } else {
        console.log(`El numero ${num} es impar`);
    }
};  

parImpar(9)
parImpar(4)

